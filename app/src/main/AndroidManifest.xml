<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.architecture.blueprints.todoapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".football.list.main.ListActivity"></activity>
        <activity
            android:name=".reference.tasks.TasksActivity"
            android:theme="@style/AppTheme.OverlapSystemBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".reference.taskdetail.TaskDetailActivity" />
        <activity android:name=".reference.addedittask.AddEditTaskActivity" />
        <activity
            android:name=".reference.statistics.StatisticsActivity"
            android:parentActivityName=".reference.tasks.TasksActivity"
            tools:ignore="UnusedAttribute">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".reference.tasks.TasksActivity" />
        </activity>
    </application>

</manifest>